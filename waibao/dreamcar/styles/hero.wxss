.hero{
    
}
.hero image{
    height: 38.2vh;
}
.hero .content{
    padding-top: 10%;
}
.hero .content.content{
    text-align: center;
}
.hero .sub-header{
    font-style: 32rpx;
    /* 只要用rpx小程序会帮我们解决适配 */
    text-transform: uppercase;
    letter-spacing: 6rpx;
    margin-bottom: 16rpx;
    color: rgb(0,0,0,0.85);
}
.hero .header{
    font-size: 72rpx;
    font-weight: bold;
    margin: 0 12% 16rpx;
}
.hero .description{
    padding:  0 12%;
    margin-bottom: 64rpx;
}